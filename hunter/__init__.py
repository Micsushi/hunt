"""
C1 (Hunter): discovery + enrichment runtime.

This package is intentionally self-contained so it can be imported for local testing
(`python -c "import hunter"`) and also executed via script entrypoints.
"""
