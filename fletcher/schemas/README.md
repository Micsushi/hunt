# Schemas

This folder contains JSON schemas for future structured LLM outputs and artifact manifests.

Initial schema set:
- `role_classification.schema.json`
- `keyword_extraction.schema.json`
- `tailored_resume.schema.json`
- `resume_attempt_manifest.schema.json`

These schemas are intentionally early-stage and may evolve as the runtime is implemented.

